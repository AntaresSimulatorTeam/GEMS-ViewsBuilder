taxonomy:
  id: my_taxonomy
  description: Minimal taxonomy

  categories:
    - id: balance
      ports:
        - id: p_balance_port
        - id: q_balance_port

    - id: consumption
      ports:
        - id: p_balance_port
        - id: q_balance_port
      properties:
        - id: business_view

    - id: production
      ports:
        - id: p_balance_port
      properties:
        - id: business_view

    - id: storage
      ports:
        - id: p_balance_port

    - id: link
      ports:
        - id: p0_port
        - id: p1_port
      properties:
        - id: business_view

    - id: coupling
      ports:
        - id: emission_port
