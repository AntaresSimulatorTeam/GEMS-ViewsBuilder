taxonomy:
  id: my_taxonomy
  description: Minimal taxonomy

  categories:
    - id: balance
      ports:
        - id: p_balance_port
        - id: q_balance_port

    - id: consumption
      ports:
        - id: p_balance_port
        - id: q_balance_port
      extra-outputs:
        - id: active_load

    - id: production
      ports:
        - id: p_balance_port
      variables:
        - id: p
        - id: pnom

    - id: storage
      ports:
        - id: p_balance_port

    - id: link
      ports:
        - id: p0_port
        - id: p1_port
      port-fields:
        - id: p0_port.flow
        - id: p1_port.flow

    - id: coupling
      ports:
        - id: emission_port
